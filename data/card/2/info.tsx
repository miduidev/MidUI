import { Metadata } from "@/lib/types";
import { Card } from "./example";

const metadata = {
  title: "Card",
  description: "An Normal Card element version 2",
  category: "card",
  component: Card,
} satisfies Metadata;

export default metadata;