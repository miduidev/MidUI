import { Metadata } from "@/lib/types";
import { Input } from "./example";

const metadata = {
    title: "Input with Icons",
    description: "An input element with Icons",
    category: "input",
    component: Input,
} satisfies Metadata;
  
export default metadata;