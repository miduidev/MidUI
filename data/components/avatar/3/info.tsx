import { Metadata } from "@/lib/types";
import { Avatar } from "./example";

const metadata = {
  title: "Text avatar",
  description: "A concise Text Avatar component with initials, using TailwindCSS, offering clear, responsive, and visually appealing representations in user profiles.",
  category: "avatar",
  component: Avatar,
} satisfies Metadata;

export default metadata;
