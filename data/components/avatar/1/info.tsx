import { Metadata } from "@/lib/types";
import { Avatar } from "./example";

const metadata = {
  title: "Avatar",
  description: "A sleek, customizable Avatar component crafted with TailwindCSS for clear, responsive, and visually appealing user profile images.",
  category: "avatar",
  component: Avatar,
} satisfies Metadata;

export default metadata;
