import { Metadata } from "@/lib/types";
import { Avatar } from "./example";

const metadata = {
  title: "Avatar with status",
  description: "A polished Avatar component with integrated status indicators, using TailwindCSS, offering clear, responsive, and visually appealing user profile images with activity status displayed as colored dots.",
  category: "avatar",
  component: Avatar,
} satisfies Metadata;

export default metadata;
