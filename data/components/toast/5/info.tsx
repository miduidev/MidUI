import { Metadata } from "@/lib/types";
import { Toast } from "./example";

const metadata = {
  title: "Error Toast",
  description: "An Error toast element without fill",
  category: "toast",
  component: Toast,
} satisfies Metadata;

export default metadata;
