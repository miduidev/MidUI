import { Metadata } from "@/lib/types";
import { Toast } from "./example";

const metadata = {
  title: "Success Toast with Fill",
  description: "An Success toast element with fill",
  category: "toast",
  component: Toast,
} satisfies Metadata;

export default metadata;
