import { Metadata } from "@/lib/types";
import { Toast } from "./example";

const metadata = {
  title: "Toast with Icon",
  description: "An toast element with icon",
  category: "toast",
  component: Toast,
} satisfies Metadata;

export default metadata;
