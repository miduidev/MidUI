import { Metadata } from "@/lib/types";
import { Toast } from "./example";

const metadata = {
  title: "Success Toast",
  description: "An Success toast element without fill",
  category: "toast",
  component: Toast,
} satisfies Metadata;

export default metadata;
