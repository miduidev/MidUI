import { Metadata } from "@/lib/types";
import { Toast } from "./example";

const metadata = {
  title: "Toast without Icon",
  description: "An toast element without icon",
  category: "toast",
  component: Toast,
} satisfies Metadata;

export default metadata;
