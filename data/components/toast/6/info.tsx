import { Metadata } from "@/lib/types";
import { Toast } from "./example";

const metadata = {
  title: "Error Toast with Fill",
  description: "An Error toast element with fill",
  category: "toast",
  component: Toast,
} satisfies Metadata;

export default metadata;
