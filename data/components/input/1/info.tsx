import { Metadata } from "@/lib/types";
import { Input } from "./example";

const metadata = {
  title: "Input",
  description: "A clean, customizable Simple Input element created with TailwindCSS for clear, responsive, and accessible text input.",
  category: "input",
  component: Input,
} satisfies Metadata;

export default metadata;
