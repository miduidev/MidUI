import { Metadata } from "@/lib/types";
import { Input } from "./example";

const metadata = {
    title: "Input with Icons",
    description: "A stylish, customizable Input with Icons element designed with TailwindCSS for clear, responsive, and accessible text input using custom SVGs.",
    category: "input",
    component: Input,
} satisfies Metadata;
  
export default metadata;