import { Metadata } from "@/lib/types";
import Example from "./example";

const metadata = {
  title: "Radio",
  description:
    "A sleek Radio component designed with TailwindCSS for clear, responsive, and user-friendly selections.",
  category: "radio",
  component: Example,
} satisfies Metadata;

export default metadata;
