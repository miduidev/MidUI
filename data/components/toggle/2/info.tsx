import { Metadata } from '@/lib/types';
import { Toggle } from './example';

const metadata: Metadata = {
  title: 'Toggle Outline',
  category: 'toggle',
  component: Toggle,
  description: 'A toggle switch with outline',
};

export default metadata;
