import { Metadata } from "@/lib/types";
import { Card } from "./example";

const metadata = {
  title: "Image Card",
  description: "An Card Element with Image",
  category: "cards",
  component: Card,
} satisfies Metadata;

export default metadata;